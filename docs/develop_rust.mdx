# Using SEGUL as a Rust crate

## Installation

To use SEGUL as a Rust library, add the following to your `Cargo.toml`:

```toml
[dependencies]
segul = "0.*"
```

Code documentation is coming soon...
